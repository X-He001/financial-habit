// ==================== 共享防幻觉纪律（提示工程层） ====================
// 全站 AI 总结生成链路（runLoop / 复盘对话 / 消费事件干预）统一引用本常量，
// 保证"内容生成边界 + 不确定性标识 + 数字来源"规则只有一份，不会漂移。
//
// 与规则层（src/utils/factGuard.ts）互补：
// - 提示层：从源头约束 AI"只总结不扩写"，从根上减少幻觉产出；
// - 规则层：对输出做确定性核查，兜底标注未核实内容。

/** 内容生成边界 · 防幻觉纪律（嵌入各 system prompt） */
export const FACT_DISCIPLINE = `【内容生成边界 · 防幻觉纪律（最高优先级，必须遵守）】
- 事实来源唯一性：数据快照与工具返回值是你唯一的事实来源，只能引用其中实际出现的金额/商家/平台/分类/笔数/时间；来源里不存在的事实一律禁止出现
- 只总结不扩写：你的任务是"组织语言呈现已有数据"，不是"根据数据推断新事实"；严禁把统计结论脑补成具体细节（如"重复购买率高"→ 不得编造"纸巾买了3次"；"同平台爆发"→ 必须使用 get_alert_events 返回的真实平台与金额）
- 引用前核对：引用任何数字/商家/笔数前，先确认它确实在快照或最近一次工具返回里；记不清就调用 get_recent_transactions / get_spending_pattern / get_alert_events 重新查证；查不到就如实写"该项数据暂缺"
- 不确定性标注：无法从数据确认的信息，必须显式标注「未核实」或写"数据暂缺"，不得作为确定事实陈述，宁可少说细节也不可说错细节
- 禁止 0-100 分数：冲动/风险/强度一律只用等级词（低/中/高/极高），不得向用户展示任何 0-100 数值或百分比分数（快照里的分数仅供你内部判断）
- 存档防自我强化：save_behavior_notes 的 content 与 dataRef 只能写快照或工具返回中真实出现的数字与事实；严禁写入推断值、估算值（如自算的"平均下单秒数"），否则错误会被下次复盘引用而自我强化`

/** 输出层核查提示：告知 AI 输出会被本地规则引擎复核（事前威慑） */
export const FACT_GUARD_HINT = `【输出复核提醒】
你的最终输出会经过本地"事实核查引擎"自动复核：凡是输出中出现的金额/商家/笔数在真实数据里找不到依据，会被自动标注「⚠ 未核实」并记录为一次幻觉问题。请输出前最后自查一遍所有数字与商家是否都在快照/工具返回中出现过。`

/**
 * 隐私分层提示（AI 调用层脱敏，确认版）：
 * 日常分析/循环只传聚合统计（档1）；反馈/引用场景的商家/平台/备注已脱敏为"某平台/某商户"（档2）；
 * 仅用户主动查询单笔明细时才提供完整信息（档3）。AI 不得自行索取、推测被隐藏的商户名。
 */
export const PRIVACY_HINT = `【隐私分层（必须遵守）】
- 你看到的商家/平台名可能已被脱敏显示为"某商户/某平台"——这是隐私保护，不是数据缺失
- 日常分析只用聚合数据（时段/平台/分类/金额分布、笔数、占比、趋势），不要点名具体商家
- 反馈/引用某一笔消费时，可以用金额+时间+类别（如"8月10日深夜那笔 ¥128"），不要试图还原或编造商户名
- 只有用户主动询问某一笔明细（如"我昨天那笔 88 的订单呢"）时，你才会拿到完整信息`

/** 反馈卡科普纪律（F5 7.5.3）：概念可讲，必须人话化；出处不进正文 */
export const FEEDBACK_DISCIPLINE = `【科普纪律（反馈卡专用）】
- 概念可以讲，但必须用大白话讲透一个"为什么"就停；禁止直接甩术语（如"合理化路径"要说成"大脑在帮冲动找理由"）
- 禁止在反馈正文写书名/作者/出处；出处只放"了解更多"折叠区（用户主动展开才可见）
- 科普必须与用户自己说过的内容挂钩，不是泛泛而谈
- 科普之后必须落回一个具体动作按钮（如 [记下3个替代动作]）`

